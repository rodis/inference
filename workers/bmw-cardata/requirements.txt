# BMW CarData subscriber (ADR 0006). Deliberately standalone — this worker is pure
# transport/auth and must NOT pull the inference package / quixstreams.
paho-mqtt>=2,<3
requests>=2.31
python-dotenv>=1
psycopg[binary]>=3.1   # persist the rotating refresh token to Neon (restart-safety)
