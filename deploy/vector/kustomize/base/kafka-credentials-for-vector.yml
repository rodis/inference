apiVersion: secrets.doppler.com/v1alpha1
kind: DopplerSecret
metadata:
  name: kafka-credentials-for-vector
  namespace: doppler-operator-system
spec:
  tokenSecret:
    name: doppler-token-secret
    namespace: doppler-operator-system
  project: kafka-credentials-for-vector
  config: prd
  managedSecret:
    name: kafka-credentials-for-vector
    namespace: vector
    type: Opaque
